{
  "name": "net-checkin",
  "version": "1.0.0",
  "description": "N2VLV Net Check-In page and tests",
  "scripts": {
    "serve": "node serve.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
